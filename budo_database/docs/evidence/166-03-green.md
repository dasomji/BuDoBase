# #166 atomic producer GREEN

Date: 2026-07-31

Direct PostgreSQL verification:

```text
python manage.py test --noinput -v 1 budo_app.kid_edit_tests.test_producer_races
python manage.py test --noinput -v 1 budo_app.kid_edit_tests.test_producer_endpoint
```

Results:

- PostgreSQL race matrix: 8/8 passed in 22.825 seconds.
- Core producer contract: 8/8 passed in 22.676 seconds.
- Both disposable test databases were destroyed cleanly.
- Django system checks, `py_compile`, and `git diff --check` passed.
- Coupled decoder, validation, lock-assuming mutation, planning, and audit
  snapshot regression selection: 94/94 passed on SQLite (2 PostgreSQL-only
  cases skipped); the PostgreSQL-only behavior is covered by the race run
  above.

The race matrix covers concurrent identical and conflicting idempotency keys,
same-child edits, number changes and uniqueness, final-seat assignment,
standalone move/remove commands, event creation/deletion, and versioned SWP
writes. Expected losers return sanitized field-addressed conflicts; no bounded
worker reached its lock timeout.
