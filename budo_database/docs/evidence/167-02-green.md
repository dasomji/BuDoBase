# #167 kid detail/edit UI GREEN

Date: 2026-07-31

Automated results:

- Focused rendered-DOM kid-edit suite: 8/8 passed.
- Django edit-shell suite: 3/3 passed.
- Relevant kid-edit, kid-detail, and route frontend selection: 88/88 passed.
- Relevant backend shell/read/detail selection: 20/20 passed.
- Vite production build passed with only the existing bundle-size warning.
- Python compilation and `git diff --check` passed.

Browser evidence used the rebuilt worktree server through the private tailnet
URL and a synthetic `Synthetic QA` child:

- Desktop viewport: 1280 × 937; four cards rendered in the required order,
  28 fixed controls plus four current dynamic selectors were present, and the
  action region computed as sticky.
- Mobile viewport: 390 × 844; all four card toggles reported expanded, the
  sticky action region remained at `bottom: 0px`, and the document had no
  horizontal overflow.
- Both screenshots were inspected individually; no real child data was used.

The implementation also amends the shared design-system guidance and accepted
frontend ADR for field-addressed validation without duplicate error toasts.
