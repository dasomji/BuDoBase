# #164-08 GREEN — audit closure

Date: 2026-07-31

## Contract alignment

The legacy audit immutability test now requires `AuditEvent` to be absent from
Django admin. Its direct save/delete/queryset mutation rejection, actor snapshot,
and Turnus cascade assertions remain intact.

## Full direct-PostgreSQL verification

A fresh unique direct PostgreSQL database was used for the complete audit test
package:

```text
python manage.py test --noinput -v 1 budo_app.audit_tests
```

Result: exit 0. Django discovery contained 82 tests; the package completed
successfully with the single intentional PostgreSQL skip for the SQLite-only
list-projection portability check. The two closure tests were included.

The execution transport omitted the trailing rendered summary after the final
progress group, so the exit status was confirmed by running the test command as
the controlling shell process. No failure or error output was emitted. Repeated
read-only database audits found zero leaked `codex_16408_full_*` or
`test_codex_16408_full_*` databases; all fresh bases and Django test databases
were removed.

Python compilation and scoped diff whitespace checks were clean. No production,
staging, commit, or push action was performed.
