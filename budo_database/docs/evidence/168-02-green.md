# #168 audit explorer and protected route GREEN

Date: 2026-07-31

Automated verification:

- Sensitive explorer: 5/5 passed.
- New and existing audit/navigation/App selection: 32/32 passed.
- Backend route, policy, list, detail, export, and bootstrap selection: 44/44
  passed.
- Full frontend rerun: 30/30 files and 317/317 tests passed. The first full
  run had one unrelated Happy Cleaning focus-timing failure; its isolated
  rerun and the complete rerun both passed.
- Final focused frontend selection: 17/17 passed.
- Vite production build and Django system checks passed.
- `python manage.py collectstatic --clear --noinput` copied 179 files.
- `git diff --check` passed.

Production-like dev/QA evidence used the built assets, an explicit Django
route, the shared dev PostgreSQL database, the private tailnet URL, a dedicated
synthetic staff account, and synthetic records only:

- `/audit` returned 301 to `/audit/`; an unauthenticated `/audit/` returned the
  exact login redirect.
- Authenticated direct load and refresh rendered the nonblank Audit-Log shell.
- Audit-Log appeared under Orgi before Admin and carried active state.
- A filtered list contained summaries only. The first sensitive reveal opened
  the privacy warning; confirmation fetched one event and rendered Allgemein,
  Gesundheitsinfos, Familie, SWP, and Happy Cleaning groups inline.
- The reveal left the page URL unchanged, wrote nothing to local/session
  storage, and emitted no sensitive toast.
- The export acknowledgement named the full sensitive categories, external
  sharing, and AI upload risk before enabling download.

The operational security gate in #165 remains blocked on human/provider
attestation. This evidence covers the implementation and dev/QA route only; it
does not authorize production exposure or merge.
