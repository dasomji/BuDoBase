# #166 core producer RED

Date: 2026-07-31

Focused PostgreSQL command:

```text
python manage.py test --noinput -v 1 budo_app.kid_edit_tests.test_producer_endpoint
```

Result: 8 tests ran in 16.539 seconds; all 8 failed at the intentionally
missing `POST /api/kids/<positive-id>/edit/` route. The test database was
destroyed after the run. The suite covers the transport/auth boundary,
canonical no-op, an all-class aggregate mutation, replay/key conflicts,
field-addressed locked errors, neutral unavailable targets, seven rollback
injection stages, and best-effort post-commit publishing. No production code
was present for this RED run.
