# #164-08 RED — audit closure remainder

Date: 2026-07-31

Scope: representative backend retention/admin tests and evidence only. No #168
frontend warning, acknowledgement, Blob lifecycle, or in-page structured reveal;
reachable `/audit` route work; production; staging; commit; or push was added.

## Backend closure contract

- `AuditEvent` is absent from Django admin, leaving list/detail/export as the
  controlled application read surfaces.
- Direct event deletion remains rejected.
- Deleting a child preserves the audit resource ID/label and complete stored
  `kid.edit` snapshots.
- Deleting the actor preserves the numeric actor ID and label snapshot.
- Deleting a Turnus cascades ordinary, `kid.edit`, `audit.view`, and
  `audit.export` rows plus its kid-edit command ledger, while another Turnus's
  audit row and ledger remain.

Repository inspection found no additional old AuditEvent HTTP full-list
serializer; the already-green list compatibility contract continues to require
summary-only rows. The registered Django admin is the remaining bypass exposed
by this RED.

## Clean direct-PostgreSQL RED

A unique direct PostgreSQL database was created around:

```text
python manage.py test --noinput -v 1 \
  budo_app.audit_tests.test_audit_closure_contract
```

Result: expected RED, exit 1. Two methods ran in 0.639 seconds with one focused
failure, no errors, no hangs, and a clean Django system check. The complete
retention lifecycle passed; the sole failure is that `AuditEvent` remains
registered in Django admin. Django destroyed its test database and the uniquely
prefixed base database was dropped successfully.

Python compilation and scoped diff whitespace checks were clean.
